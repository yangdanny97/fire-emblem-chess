{
  "devDependencies": {
    "bs-platform": "^9.0.2",
    "rescript": "^10.0.1"
  },
  "scripts": {
    "build": "rescript",
    "start": "rescript build -w",
    "bundle": "esbuild src/Game.js --outfile=out/game.js --bundle"
  },
  "dependencies": {
    "esbuild": "^0.15.8"
  }
}
